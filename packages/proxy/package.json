{
  "name": "@paybound/proxy",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "tsup": "^8.0.0",
    "vitest": "^3.0.0"
  }
}
